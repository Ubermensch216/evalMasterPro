# 🏆 EvalMaster Pro (이발마스터 프로)

> **정확하고 공정한 심사를 위한 실시간 평가·채점 및 결과 집계 웹 애플리케이션**

EvalMaster Pro는 각종 공모전, 해커톤, 제안서 심사, 채용 면접, 인사 평가 등에서 여러 명의 평가위원이 실시간으로 대상자를 채점하고, 관리자가 이를 종합하여 순위와 통계를 즉시 산출할 수 있는 전문 평가 솔루션입니다.

---

## 🌟 주요 핵심 기능

### 1. 📝 평가위원 실시간 채점 (`/evaluate`)
- **간편한 로그인**: 드롭다운에서 본인 이름을 선택하고 전용 비밀번호를 입력하여 접속
- **직관적인 채점 UI**: 평가 항목(루브릭)별 배점 슬라이더 및 숫자 직접 입력 지원
- **심사 소견/의견 작성**: 대상자별 최대 300자의 정성적 심사 의견 실시간 기록
- **채점 잠금(Lock)**: 모든 채점을 완료한 후 제출하면 데이터 오작동 및 수정 방지
- **진행 현황 요약표**: 자신이 채점한 모든 대상자의 점수와 진행률을 한눈에 조회

### 2. 🛡️ 관리자 대시보드 (`/admin`)
- **이원화된 보안 체계**: 일반 관리자 비밀번호 + 비상 복구용 슈퍼 마스터 비밀번호(`0132`) 지원
- **평가위원 관리**: 평가위원 추가/수정/삭제, 개별 비밀번호 관리, 채점 잠금 강제 해제/설정
- **평가 대상자 관리**: 심사 대상자/후보자 개별 등록 및 대량 일괄 등록 지원
- **평가 항목 및 배점표 설정**: 심사 기준(항목명) 추가 및 항목별 가중치/최대 배점(`maxScore`) 설정
- **공식 인쇄 서식 (`PrintableView`)**: 하단에 평가위원 공식 서명란이 포함된 A4 심사표 즉시 인쇄/PDF 저장
- **데이터 관리**: 새로운 심사 회차를 위한 데이터 백업 및 초기화(시딩) 기능 제공

### 3. 📊 종합 채점 결과 및 통계 (`/results`)
- **결과 열람 보안**: 전용 비밀번호를 통한 안전한 결과 보호
- **실시간 자동 랭킹**: 총점 및 평균 점수 기준 실시간 순위(등수) 산출
- **세부 내역 팝업**: 각 대상자별로 어떤 평가위원이 몇 점을 주었는지 상세 채점표 조회

---

## 🔑 초기 접속 정보 및 기본 비밀번호

시스템 최초 실행 시 아래의 기본 설정값이 자동으로 구성됩니다:

| 구분 | 초기 계정 / 명칭 | 초기 비밀번호 | 비고 |
| :--- | :--- | :--- | :--- |
| **슈퍼 마스터 관리자** | 마스터 권한 | `0132` | 비밀번호 분실 시 비상 복구용 (불변) |
| **일반 관리자 (`/admin`)** | 시스템 관리자 | `1` | 관리자 페이지에서 변경 가능 |
| **결과 조회 (`/results`)** | 결과 열람 권한 | `1` | 관리자 페이지에서 변경 가능 |
| **기본 평가위원 (`/evaluate`)** | `김평가`, `이평가`, `박평가` | `1` | 관리자 페이지에서 수정 및 추가 가능 |

---

## 📖 사용자별 단계별 이용 가이드

### 👤 [1] 관리자 가이드 (대회/심사 준비 및 총괄)
1. **접속**: 메인 화면에서 `관리자` 카드를 클릭하거나 `/admin`으로 이동합니다.
2. **로그인**: 관리자 비밀번호(`1`) 또는 슈퍼 비밀번호(`0132`)를 입력합니다.
3. **심사 기준 설정**:
   - `평가 항목 관리` 탭에서 심사 지표(예: 창의성, 기술성, 발표력 등)와 각 항목의 최대 배점을 입력합니다.
4. **대상자 및 심사위원 등록**:
   - `평가 대상자 관리` 탭에서 발표자/팀명을 등록합니다.
   - `평가위원 관리` 탭에서 심사에 참여할 평가위원 목록을 생성하고 전달할 임시 비밀번호를 설정합니다.
5. **심사 종료 후 집계**:
   - 모든 평가위원이 채점을 마치면 `결과 집계` 탭에서 최종 순위를 확인합니다.
   - `심사표 인쇄` 버튼을 눌러 평가위원별 서명이 포함된 결과지를 A4로 출력하거나 PDF로 보관합니다.

### ✍️ [2] 평가위원 가이드 (실시간 채점)
1. **접속**: 메인 화면에서 `평가 위원` 카드를 클릭하거나 `/evaluate`로 이동합니다.
2. **로그인**: 드롭다운에서 본인의 성명을 선택하고 부여받은 비밀번호를 입력합니다.
3. **채점 진행**:
   - 화면에 표시된 대상자별로 평가 항목의 슬라이더를 조절하거나 점수를 입력합니다.
   - 필요한 경우 하단 '기타 의견' 란에 심사 소견을 작성합니다 (자동 저장).
4. **채점 완료 및 제출**:
   - 모든 대상자의 채점이 끝나면 `채점 완료 및 제출` 버튼을 누릅니다.
   - 제출 후에는 점수가 잠금 상태가 되며, 수정이 필요한 경우 관리자에게 잠금 해제를 요청합니다.

### 🏆 [3] 결과 열람자 가이드 (순위 확인)
1. **접속**: 메인 화면에서 `채점 결과` 카드를 클릭하거나 `/results`로 이동합니다.
2. **비밀번호 입력**: 결과 열람 비밀번호(`1`)를 입력합니다.
3. **확인**: 총점과 평균 기준 순위표를 확인하고, 대상자를 클릭하여 평가위원별 세부 배점을 확인합니다.

---

## 💻 로컬 개발 환경 설정 (Local Setup)

### 1. 필수 사전 요구사항
- **Node.js**: `v20.0.0` 이상 권장
- **npm**: `v10.0.0` 이상

### 2. 환경 변수 설정 (`.env.local`)
프로젝트 루트 경로에 `.env.local` 파일을 생성하고 Firebase 프로젝트 설정을 입력합니다:

```env
NEXT_PUBLIC_FIREBASE_API_KEY="AIzaSy..."
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your-project.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="your-project"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="your-project.firebasestorage.app"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="123456789012"
NEXT_PUBLIC_FIREBASE_APP_ID="1:123456789012:web:abcdef..."
```

### 3. 패키지 설치 및 개발 서버 실행
```bash
# 의존성 패키지 설치
npm install

# 타입 검사
npm run typecheck

# 로컬 개발 서버 실행 (기본 포트: 9002)
npm run dev
```

브라우저에서 `http://localhost:9002` (또는 터미널에 표시된 포트)로 접속합니다.

---

## 🚀 배포 가이드 (Production Deployment)

### 방법 A: Firebase App Hosting 배포 (추천)
프로젝트에 포함된 [`apphosting.yaml`](file:///d:/Dev/evalMasterPro/apphosting.yaml) 설정을 통해 Firebase App Hosting으로 손쉽게 배포할 수 있습니다:
1. GitHub 저장소에 코드를 푸시합니다.
2. [Firebase 콘솔](https://console.firebase.google.com/) > `빌드` > `App Hosting`으로 이동합니다.
3. GitHub 저장소를 연결하고 브랜치를 선택하여 자동 빌드 및 배포를 완료합니다.

### 방법 B: Vercel 배포
1. [Vercel](https://vercel.com/)에 프로젝트를 임포트합니다.
2. **Environment Variables**에 `.env.local`의 `NEXT_PUBLIC_FIREBASE_*` 환경변수 6개를 모두 등록합니다.
3. `Deploy` 버튼을 누르면 즉시 글로벌 CDN으로 배포됩니다.

### 🔒 Firestore 보안 규칙 배포 (`firestore.rules`)
Firebase CLI를 통해 데이터베이스 보안 규칙을 배포합니다:
```bash
firebase deploy --only firestore:rules
```

---

## ❓ 자주 묻는 질문 (FAQ & Troubleshooting)

**Q. 관리자 비밀번호를 잊어버렸습니다.**
> 비상 복구용 **슈퍼 마스터 비밀번호 `0132`**를 입력하시면 언제든지 관리자 페이지에 접근할 수 있으며, 설정 패널에서 일반 비밀번호를 새로 변경하실 수 있습니다.

**Q. 평가위원이 채점 제출 후 점수를 수정하고 싶어합니다.**
> 관리자 페이지(`/admin`)의 `평가위원 관리` 탭에서 해당 평가위원의 **채점 잠금 해제(자물쇠 아이콘)**를 클릭하면 평가위원이 다시 점수를 수정할 수 있습니다.

**Q. 새로운 대회를 위해 데이터를 모두 비우고 싶습니다.**
> 관리자 페이지(`/admin`)의 `설정` 패널 하단에 있는 **[데이터 초기화]** 버튼을 누르면 모든 평가위원, 대상자, 점수 데이터가 초기 상태로 안전하게 리셋됩니다.

---

## 🛠️ 기술 스택 상세

- **Framework**: Next.js 15.3.8 (App Router), React 18
- **Language**: TypeScript 5
- **Database**: Google Cloud Firestore (Real-time `onSnapshot`)
- **Styling**: Tailwind CSS, Radix UI (shadcn/ui), Lucide React
- **Visualization**: Recharts
- **AI Integration**: Google Genkit (`@genkit-ai/googleai`)
